var animation = bodymovin.loadAnimation({
    container: document.getElementById('location'),
    renderer: 'canvas',
    loop: false,
    autoplay: true,
    path: 'json/previous_location.json'
  })

 
